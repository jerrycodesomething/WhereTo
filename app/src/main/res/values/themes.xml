<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.WhereTo" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/teal_700</item>
        <item name="colorPrimaryVariant">@color/teal_200</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="21">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name ="navigationTheme">
        <!-- Color of text & icon when selected -->
        <item name="colorPrimary">@color/white</item>

        <!-- Background color when selected -->
        <item name="colorControlHighlight">@color/teal_200</item>
    </style>

    <style name ="overlayBottomDrawerTheme" parent = "Theme.Design.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/overlayBottomDrawerStyle</item>
    </style>

    <style name= "overlayBottomDrawerStyle" parent = "Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>

</resources>